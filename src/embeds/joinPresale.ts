import { embedFactory } from './embedFactory';

export const joinPresaleEmbed = embedFactory({
  description:
    'Run `!join-presale` to receive instructions about joining the presale.',
  severity: 'success',
  title: 'Join the presale!  🍦',
});
